<% if flash[:user_error] %>
    <div><% id="error_explanation"%>
        <h2>
            <%= pluralize(flash[:user_error].count, "error")%>
            stopped this Rubber Duck from being saved
        </h2>

        <ul>
        <% flash[:user_error].each do |msg|  %>
            <li><%= msg %></li>
        <% end %>
        </ul>
    </div>
<% end %>

<%= form_for @duck do |f| %>

<label>Rubber Duck's Name:</label>
<%= f.text_field :name %><br><br>

<label>Rubber Duck's Description:</label><br>
<%= f.text_area :description, :cols => "100", :rows => "10" %><br><br>

<label>Rubber Duck's Description:</label>
<%= f.collection_select :student_id, Student.all, :id, :name %><br><br>

<%= f.submit "Save Your Duck" %>
<% end %>